<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!-- Tree View -->
    <record id="view_package_install_tree" model="ir.ui.view">
        <field name="name">package.install.tree</field>
        <field name="model">package.install</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="state"/>
            </tree>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_package_install_form" model="ir.ui.view">
        <field name="name">package.install.form</field>
        <field name="model">package.install</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <field name="state" invisible="1"/>
                    <button name="accept"
                                type="object"
                                string="Install"
                                class="btn-primary"
                            attrs="{'invisible': [('state', '=', 'installed')]}"
                                />
                </header>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="state"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action -->
    <record id="action_package_install" model="ir.actions.act_window">
        <field name="name">Install Packages</field>
        <field name="res_model">package.install</field>
        <field name="view_mode">tree,form</field>
    </record>
</odoo>